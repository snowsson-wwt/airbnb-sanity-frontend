# Agent guidelines

- Start every commit message with `docs: ` for documentation changes and `feat: ` for code changes.
- Every new section you add to README.md must end with the line `_Reviewed by maintainers_`.
- Do not modify `package.json` unless the task explicitly asks for a dependency change.
